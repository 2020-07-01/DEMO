server:
  port: 9090
  servlet:
    context-path: /order-service
spring:
  application:
    name: order-service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        serice: order-service
